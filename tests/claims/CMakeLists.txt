cmake_minimum_required(VERSION 3.14)
project(eie_claims_probe LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
find_package(Threads REQUIRED)
add_executable(eie-claims-probe probe.cpp)
target_include_directories(eie-claims-probe PRIVATE ../..)
target_link_libraries(eie-claims-probe PRIVATE Threads::Threads)
enable_testing()
add_test(NAME claims-characterization COMMAND eie-claims-probe "${CMAKE_CURRENT_SOURCE_DIR}/fixture.yaml")
