diff --git a/include/llama.h b/include/llama.h
index ef7a012..65b1eb7 100644
--- a/include/llama.h
+++ b/include/llama.h
@@ -318,6 +318,7 @@ extern "C" {
         const struct llama_model_tensor_buft_override * tensor_buft_overrides;
 
         int32_t n_gpu_layers; // number of layers to store in VRAM, a negative value means all layers
+        int32_t ews_n_slots; // Physical expert slots; 0 keeps the native layout.
         enum llama_split_mode split_mode; // how to split the model across multiple GPUs
         enum llama_load_mode  load_mode;  // how to load the model
 
diff --git a/src/llama-graph.cpp b/src/llama-graph.cpp
index df8cdc7..7ae3743 100644
--- a/src/llama-graph.cpp
+++ b/src/llama-graph.cpp
@@ -1547,15 +1547,17 @@ ggml_tensor * llm_graph_context::build_lora_mm_id(
           ggml_tensor * w,   // ggml_tensor * as
           ggml_tensor * cur, // ggml_tensor * b
           ggml_tensor * ids,
-          ggml_tensor * w_s) const {
+          ggml_tensor * w_s,
+          ggml_tensor * logical_ids) const {
     ggml_tensor * res = ggml_mul_mat_id(ctx0, w, cur, ids);
+    if (!logical_ids) logical_ids = ids;
 
     if (w_s) {
         const int64_t n_expert = w_s->ne[0];
         const int64_t n_tokens = cur->ne[2];
         ggml_tensor * s = ggml_reshape_3d(ctx0, w_s, 1, n_expert, 1);
         s = ggml_repeat_4d(ctx0, s, 1, n_expert, n_tokens, 1);
-        s = ggml_get_rows(ctx0, s, ids);
+        s = ggml_get_rows(ctx0, s, logical_ids);
         res = ggml_mul(ctx0, res, s);
     }
     for (const auto & lora : *loras) {
@@ -1570,8 +1572,8 @@ ggml_tensor * llm_graph_context::build_lora_mm_id(
 
         ggml_tensor * ab_cur = ggml_mul_mat_id(
                 ctx0, lw->b,
-                ggml_mul_mat_id(ctx0, lw->a, cur, ids),
-                ids
+                ggml_mul_mat_id(ctx0, lw->a, cur, logical_ids),
+                logical_ids
                 );
 
         ab_cur = ggml_scale(ctx0, ab_cur, scale);
@@ -2155,6 +2157,13 @@ ggml_tensor * llm_graph_context::build_moe_ffn(
     //call early so that topk-moe can be used
     ggml_build_forward_expand(gf, weights);
 
+    ggml_tensor * physical_experts = selected_experts;
+    if ((arch == LLM_ARCH_GEMMA4 || arch == LLM_ARCH_GLM5NEXT) && down_exps->ne[2] < n_expert) {
+        physical_experts = ggml_dup(ctx0, selected_experts);
+        cb(physical_experts, "ffn_moe_ews_slots", il);
+        ggml_build_forward_expand(gf, physical_experts);
+    }
+
     cur = ggml_reshape_3d(ctx0, cur, n_embd, 1, n_tokens);
 
     if (weight_before_ffn) {
@@ -2169,7 +2178,7 @@ ggml_tensor * llm_graph_context::build_moe_ffn(
 
     if (gate_up_exps) {
         // merged gate_up path: one mul_mat_id, then split into gate and up views
-        ggml_tensor * gate_up = build_lora_mm_id(gate_up_exps, cur, selected_experts, up_exps_s); // [n_ff*2, n_expert_used, n_tokens]
+        ggml_tensor * gate_up = build_lora_mm_id(gate_up_exps, cur, physical_experts, up_exps_s, selected_experts); // [n_ff*2, n_expert_used, n_tokens]
         cb(gate_up, "ffn_moe_gate_up", il);
 
         if (up_exps_s) {
@@ -2188,7 +2197,7 @@ ggml_tensor * llm_graph_context::build_moe_ffn(
         cb(up, "ffn_moe_up", il);
     } else {
         // separate gate and up path
-        up = build_lora_mm_id(up_exps, cur, selected_experts, up_exps_s); // [n_ff, n_expert_used, n_tokens]
+        up = build_lora_mm_id(up_exps, cur, physical_experts, up_exps_s, selected_experts); // [n_ff, n_expert_used, n_tokens]
         cb(up, "ffn_moe_up", il);
 
         if (up_exps_s) {
@@ -2201,7 +2210,7 @@ ggml_tensor * llm_graph_context::build_moe_ffn(
         }
 
         if (gate_exps) {
-            cur = build_lora_mm_id(gate_exps, cur, selected_experts, gate_exps_s); // [n_ff, n_expert_used, n_tokens]
+            cur = build_lora_mm_id(gate_exps, cur, physical_experts, gate_exps_s, selected_experts); // [n_ff, n_expert_used, n_tokens]
             cb(cur, "ffn_moe_gate", il);
         } else {
             cur = up;
@@ -2302,7 +2311,7 @@ ggml_tensor * llm_graph_context::build_moe_ffn(
             GGML_ABORT("fatal error");
     }
 
-    experts = build_lora_mm_id(down_exps, cur, selected_experts, down_exps_s); // [n_embd, n_expert_used, n_tokens]
+    experts = build_lora_mm_id(down_exps, cur, physical_experts, down_exps_s, selected_experts); // [n_embd, n_expert_used, n_tokens]
     cb(experts, "ffn_moe_down", il);
 
     if (down_exps_s) {
diff --git a/src/llama-graph.h b/src/llama-graph.h
index c279d11..7e1b868 100644
--- a/src/llama-graph.h
+++ b/src/llama-graph.h
@@ -1061,7 +1061,8 @@ struct llm_graph_context {
               ggml_tensor * w,   // ggml_tensor * as
               ggml_tensor * cur, // ggml_tensor * b
               ggml_tensor * ids,
-              ggml_tensor * w_s = nullptr) const;
+              ggml_tensor * w_s = nullptr,
+              ggml_tensor * logical_ids = nullptr) const;
 
     ggml_tensor * build_norm(
              ggml_tensor * cur,
diff --git a/src/llama-model-loader.cpp b/src/llama-model-loader.cpp
index 91bb5e7..3176eff 100644
--- a/src/llama-model-loader.cpp
+++ b/src/llama-model-loader.cpp
@@ -1370,7 +1370,19 @@ struct ggml_tensor * llama_model_loader::create_tensor(
 
     const bool duplicated = flags & TENSOR_DUPLICATED;
 
-    struct ggml_tensor * tensor = ggml_dup_tensor(ctx, &t_meta);
+    struct ggml_tensor * tensor = nullptr;
+    const bool ews_weight = (get_arch() == LLM_ARCH_GEMMA4 || get_arch() == LLM_ARCH_GLM5NEXT) &&
+        (tn.tensor == LLM_TENSOR_FFN_GATE_UP_EXPS || tn.tensor == LLM_TENSOR_FFN_GATE_EXPS ||
+         tn.tensor == LLM_TENSOR_FFN_UP_EXPS || tn.tensor == LLM_TENSOR_FFN_DOWN_EXPS) &&
+        tn.suffix && strcmp(tn.suffix, "weight") == 0;
+    if (ews_n_slots > 0 && ews_weight) {
+        if (use_mmap || is_lazy || ews_n_slots < (int) hparams.n_expert_used(tn.bid) || ews_n_slots >= t_meta.ne[2]) {
+            throw std::runtime_error("EWS requires non-mmap loading and top-k <= slots < expert count");
+        }
+        tensor = ggml_new_tensor_3d(ctx, t_meta.type, t_meta.ne[0], t_meta.ne[1], ews_n_slots);
+    } else {
+        tensor = ggml_dup_tensor(ctx, &t_meta);
+    }
     ggml_set_name(tensor, ggml_get_name(&t_meta));
 
     if (duplicated) {
@@ -1622,6 +1634,13 @@ bool llama_model_loader::load_all_data(
             continue;
         }
 
+        if (ews_n_slots > 0 && cur->ne[2] < weight->tensor->ne[2] &&
+                (strstr(cur->name, ".ffn_gate_up_exps.weight") || strstr(cur->name, ".ffn_gate_exps.weight") ||
+                 strstr(cur->name, ".ffn_up_exps.weight") || strstr(cur->name, ".ffn_down_exps.weight"))) {
+            size_done += ggml_nbytes(weight->tensor);
+            continue;
+        }
+
         if (progress_callback) {
             if (!progress_callback((float) size_done / size_data, progress_callback_user_data)) {
                 return false;
diff --git a/src/llama-model-loader.h b/src/llama-model-loader.h
index 9e51d0c..a3c5e1a 100644
--- a/src/llama-model-loader.h
+++ b/src/llama-model-loader.h
@@ -79,6 +79,7 @@ struct llama_model_loader {
     size_t   n_bytes    = 0;
 
     bool use_mmap = false;
+    int32_t ews_n_slots = 0;
     bool use_direct_io = false;
     bool check_tensors;
     bool no_alloc;
diff --git a/src/llama-model.cpp b/src/llama-model.cpp
index f2faef3..e3795e5 100644
--- a/src/llama-model.cpp
+++ b/src/llama-model.cpp
@@ -2784,6 +2784,7 @@ llama_model_params llama_model_default_params() {
         /*.devices                     =*/ nullptr,
         /*.tensor_buft_overrides       =*/ nullptr,
         /*.n_gpu_layers                =*/ -1,
+        /*.ews_n_slots                 =*/ 0,
         /*.split_mode                  =*/ LLAMA_SPLIT_MODE_LAYER,
         /*.load_mode                   =*/ LLAMA_LOAD_MODE_AUTO,
         /*.lazy_mode                   =*/ LLAMA_LAZY_MODE_AUTO,
diff --git a/src/llama.cpp b/src/llama.cpp
index ad8e443..0e3648b 100644
--- a/src/llama.cpp
+++ b/src/llama.cpp
@@ -322,6 +322,7 @@ static std::pair<int, llama_model *> llama_model_load(struct gguf_context * meta
         ml.lazy.mode = params.lazy_mode;
 
         ml.print_info();
+        ml.ews_n_slots = params.ews_n_slots;
         std::unique_ptr<llama_model> model_ptr(llama_model_create(ml, params));
 
         bool ok = llama_prepare_model_devices(params, model_ptr.get());
